export default {
  // Uncomment the following line to debug i18n:
  // debug: process.env.NODE_ENV === 'development',
  fallbackLng: {
    // if this config is missing, de doesn't fallback to de-DE
    de: ['de-DE'],
    default: ['en-US'],
  },
  load: 'all',
};
