import { connect } from 'react-redux';
import CommunitySettings from '@/components/pages/community-settings';

const mapStateToProps = () => ({});
const mapActionsToProps = {};

export default connect(
  mapStateToProps,
  mapActionsToProps,
)(CommunitySettings);
