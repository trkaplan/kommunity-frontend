import { connect } from 'react-redux';
import CommunityDiscover from '@/components/pages/community-discover';

const mapStateToProps = () => ({});
const mapActionsToProps = {};

export default connect(
  mapStateToProps,
  mapActionsToProps,
)(CommunityDiscover);
